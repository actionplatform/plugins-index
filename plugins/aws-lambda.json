{
  "name": "aws-lambda",
  "description": "Deploy to AWS Lambda with SAM; read CloudFormation stacks and functions",
  "author": "actionplatform",
  "verified": true,
  "repo": "https://github.com/actionplatform/apx-aws-lambda",
  "pypi": "apx-aws-lambda",
  "latest": "0.2.0",
  "min_core": "0.17",
  "needs": [
    "tool: aws, sam (bundled as Python packages when not on PATH)",
    "env: AWS_REGION; credentials from role_arn (OIDC, no keys) or the AWS CLI chain (SSO, profile, instance role)",
    "net: *.amazonaws.com"
  ],
  "tags": [
    "cloud",
    "aws",
    "lambda"
  ]
}
